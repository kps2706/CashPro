<?php

// $select_img = $pdo->prepare("SELECT * FROM tblimg");

// $select_img->execute();

// while ($row = $select_img->fetch(PDO::FETCH_ASSOC)){

// 	//echo var_dump($row['image']);

// 	echo "<img src=data:image/jpeg;base64,".base64_encode($row['image'])." width='250' height='200' />";

// }

// $industry = "2/3 Wheelers";

// $cnt = 0;

// $select = $pdo->prepare("SELECT MAX(cmp_date) as cmp_date FROM tbl_cmp_price where ISIN_Code='INE192A01025'");

// $select->execute();

// $cmp_date_db = NULL;


// while ($row = $select->fetch(pdo::FETCH_OBJ)) {

// 	// var_dump($row->cmp_date);

// 	$cmp_date_db = $row->cmp_date;
// }

// echo $cmp_date_db;

// page is refresh and data is send for second time


                                                // session_destroy();

                                                // echo '<script>

                                                // window.location.href = "./login.php";

                                                // </script>';

                                                // header('location:../logout.php');

                                                // echo 'coming second';

                                                